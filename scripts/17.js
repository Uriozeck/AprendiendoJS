// Metodo y Función

const numero1 = 20;
const numero2 = '20';

console.log(parseInt(numero2)); //parseint es una funcion

console.log(numero1.toString()); // .toString es un método

/**Diferencia entre metodos y funciones en JS
 * 
 * Las funciones se encuentran en el objeto window  y dentro de estas en sus parétesis contienen un valor
 * los metodos tienen el nombre de la variable o del objeto seguido de un punto.
 */